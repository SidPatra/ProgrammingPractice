#num1 and num2 are called arguments, seperable by comma
def add(num1, num2): 
    return num1 + num2 #addition
def sub(num1, num2):
    return num1 - num2 #subtraction
def mul(num1, num2):
    return num1 * num2 #multiplication
def div(num1, num2):
    return num1 / num2 #division
def mainsub1()
	var2 = int(input("Enter num2: ")	
			if(operation == "+"):
				def presolve = (add(var1, var2))
			elif(operation == "-"):
				def presolve = (sub(var1, var2))
			elif(operation == "*"):
				def presolve = (mul(var1, var2))
			elif(operation == "/"):
				def presolve = (div(var1, var2))
	var1 = presolve
def mainsub2()
	var2 = int(input("Enter num2: ")	
			if(operation == "+"):
				def presolve = (add(var1, var2))
			elif(operation == "-"):
				def presolve = (sub(var1, var2))
			elif(operation == "*"):
				def presolve = (mul(var1, var2))
			elif(operation == "/"):
				def presolve = (div(var1, var2))
			elif(operation == "enter")
				print presolve

var1 = int(input("Enter num1: "))
def main()
	operation = input("what do you want to do? +,-,*,/?"):
	if (operation != '+' and operation != '-' and operation != '*' and operation != '/'):
		print ("enter a valid operation")
	else:
		mainsub1()
	operation = input("what do you want to do? +,-,*,/,enter?"):
		if (operation != '+' and operation != '-' and operation != '*' and operation != '/' and operation != 'enter'):
			print ("enter a valid operation")
		else:
		mainsub2()
while(True)
	main()